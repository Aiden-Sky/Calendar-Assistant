<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- 添加存储权限，支持头像上传 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design"
        tools:targetApi="31">
        <activity
            android:name=".activities.Edit_profile_activity"
            android:exported="false" />
        <activity
            android:name=".activities.Login_activity"
            android:exported="true"
            android:theme="@style/Theme.Design">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.Home_activity"
            android:exported="true"
            android:theme="@style/Theme.Design" /> <!-- 注册其他 Activity -->
        <activity android:name=".activities.login_rigist_activity" />
        <activity android:name=".activities.login_forget_activity" />


    </application>

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"
        tools:ignore="ManifestOrder" />

</manifest>
